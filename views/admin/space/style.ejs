<style>
  .page-space-wrap {
    position: relative;
    min-height: calc(100vh - 120px);
  }

  .space-add-number {
    width: 100%;
  }

  /* menu-list */

  .space-menu-list-wrap {
    width: 200px;
    padding: 15px 0;
  }

  .space-tree-node {
    box-sizing: border-box;
    display: flex;
    align-items: center;
    width: 100%;
    height: 100%;
    padding-right: 5px;
    font-size: 14px;
  }

  .space-tree-node.active {
    color: var(--el-color-primary);
  }

  .space-tree-text {
    flex: 1;
  }

  /* current-page */
  .current-page-wrap .current-page-title {
    font-size: 16px;
    font-weight: 700;
    margin-bottom: 20px;
  }

  .form-content-radio {
    width: 120px;
    margin: 0 10px 5px !important;
  }
</style>
