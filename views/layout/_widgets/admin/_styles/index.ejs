<style>
  html, body {
    background-color: #f3f3f3;
  }
  .admin-nav-top {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    text-align: right;
    z-index: 990;
  }

  .admin-nav-top .nav-user-icon {
    width: 40px;
    height: 40px;
    display: inline-block;
    vertical-align: middle;
  }
  .admin-nav-top .flex-grow-place {
    flex-grow: 1;
  }

  .admin-nav-left {
    position: fixed;
    top: 0;
    bottom: 0;
    left: 0;
    overflow-x: hidden;
    z-index: 999;
  }

  .admin-nav-left .admin-nav-left-menu {
    height: 100%;
  }

  .admin-nav-left .layui-nav-logo {
    width: 100%;
    height: 60px;
    line-height: 60px;
    font-size: 28px;
    color: #ffffff;
    text-align: center;
  }

  .admin-nav-left .layui-nav-logo-img {
    height: 60px;
  }

  .page-container {
    box-sizing: border-box;
    margin-top: 75px;
    margin-left: 235px;
    margin-right: 15px;
    padding: 15px;
    background-color: #ffffff;
  }
</style>
